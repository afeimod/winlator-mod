name: winlator GStreamer Decoders (aarch64, Dev + libvpx) - 编译器修复版

on:
  workflow_dispatch:
  schedule:
    - cron: '0 7 * * *'
  pull_request:

permissions:
  contents: write
  packages: write

env:
  LAST_COMMIT_FILE: last_gst_commit.txt
  INSTALL_ROOT: /data/data/com.winlator/files/rootfs/usr

jobs:
  build:
    runs-on: ubuntu-24.04-arm

    steps: 
      # 🧩 检出仓库
      - name: 检出 workflow 仓库
        uses: actions/checkout@v4

      # 📦 安装基础依赖
      - name: 安装基础依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3-pip git pkg-config nasm \
            libglib2.0-dev libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            liborc-0.4-dev libxv-dev libx11-dev libasound2-dev libpulse-dev patchelf \
            libtheora-dev libx264-dev libx265-dev libvpx-dev

      # ⚙️ 升级 Meson 和 Ninja
      - name: 升级 Meson 和 Ninja
        run: |
          pip install --upgrade pip
          pip install --upgrade meson ninja

      # 🔧 设置 Winlator 编译与链接路径
      - name: 设置 Winlator 编译环境
        run: |
          WINLATOR_ROOT="/data/data/com.winlator/files/rootfs"
          WINLATOR_LIB="${WINLATOR_ROOT}/usr/lib"
          WINLATOR_INC="${WINLATOR_ROOT}/usr/include"

          # 优化后的RPATH设置
          OPTIMIZED_RPATH='$ORIGIN:$ORIGIN/..:$ORIGIN/../gstreamer-1.0:$ORIGIN/../../../gstreamer/libs/gst/base:$ORIGIN/../../../gstreamer/gst:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/pbutils:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/video:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/audio:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/tag:$ORIGIN/../../../glib-2.82.2/glib:$ORIGIN/../../../glib-2.82.2/gobject:$ORIGIN/../../../glib-2.82.2/gmodule:$ORIGIN/../../../libffi/src:$ORIGIN/../../../orc/orc:${WINLATOR_LIB}'

          echo "LDFLAGS='-L${WINLATOR_LIB} -Wl,--dynamic-linker=${WINLATOR_LIB}/ld-linux-aarch64.so.1 -Wl,-rpath=${OPTIMIZED_RPATH}'" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH='${WINLATOR_LIB}:\$LD_LIBRARY_PATH'" >> $GITHUB_ENV
          echo "PKG_CONFIG_PATH='${WINLATOR_LIB}/pkgconfig:${WINLATOR_LIB}/aarch64-linux-gnu/pkgconfig:\$PKG_CONFIG_PATH'" >> $GITHUB_ENV
          echo "OPTIMIZED_RPATH=${OPTIMIZED_RPATH}" >> $GITHUB_ENV

          echo "✅ 已设置优化的 Winlator 环境变量"

      # 🧬 克隆 GStreamer 开发版本
      - name: 克隆 GStreamer 开发版本
        run: git clone --depth=1 https://gitlab.freedesktop.org/gstreamer/gstreamer.git

      # 🔍 获取开发版本信息
      - name: 获取开发版本信息
        id: get_dev_info
        run: |
          cd gstreamer
          LATEST_COMMIT=$(git rev-parse HEAD)
          COMMIT_SHORT=$(git rev-parse --short HEAD)
          AUTHOR=$(git log -1 --pretty=format:'%an')
          DATE=$(date -u '+%Y-%m-%d %H:%M UTC')
          COMMIT_DATE=$(git log -1 --pretty=format:'%cd' --date=short)
          VERSION=$(sed -n '2p' "meson.build" | grep -oP "version\\s*:\\s*'\\K[^']+")
          echo "LATEST_COMMIT=$LATEST_COMMIT" >> $GITHUB_ENV
          echo "COMMIT_SHORT=$COMMIT_SHORT" >> $GITHUB_ENV
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "AUTHOR=$AUTHOR" >> $GITHUB_ENV
          echo "BUILD_DATE=$DATE" >> $GITHUB_ENV
          echo "COMMIT_DATE=$COMMIT_DATE" >> $GITHUB_ENV

      # 💾 获取上次构建提交
      - name: 获取上次构建提交
        id: get_last_commit
        run: |
          FILE=${{ env.LAST_COMMIT_FILE }}
          if [ -f "$FILE" ]; then
            LAST_COMMIT=$(cat "$FILE")
          else
            echo "none" > "$FILE"
            LAST_COMMIT="none"
          fi
          echo "LAST_COMMIT=$LAST_COMMIT" >> $GITHUB_ENV

      # 🔍 判断是否需要构建
      - name: 判断是否需要构建
        id: check_build
        run: |
          if [ "$LATEST_COMMIT" = "$LAST_COMMIT" ]; then
            echo "skip_build=true" >> $GITHUB_OUTPUT
            echo "🟡 提交未更新，跳过构建。"
          else
            echo "skip_build=false" >> $GITHUB_OUTPUT
            echo "✅ 发现新提交: $LATEST_COMMIT"
          fi

      # ⏭️ 跳过无更新构建
      - name: 跳过无更新构建
        if: steps.check_build.outputs.skip_build == 'true'
        run: |
          echo "🟡 代码无更新，跳过构建流程"
          exit 0

      # 🔐 准备安装目录权限
      - name: 准备安装目录权限
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "🔐 准备安装目录权限..."
          WINLATOR_ROOT="/data/data/com.winlator/files/rootfs"
          
          # 创建完整的目录结构
          sudo mkdir -p $WINLATOR_ROOT/usr/{lib,include,bin,share}
          
          # 设置正确的所有权和权限
          sudo chown -R $USER:$USER $WINLATOR_ROOT
          sudo chmod -R 755 $WINLATOR_ROOT
          
          # 验证权限
          echo "📁 目录权限:"
          ls -la $WINLATOR_ROOT/
          ls -la $WINLATOR_ROOT/usr/
          echo "✅ 安装目录权限设置完成"

      # 🧱 构建 libvpx v1.14.0（修复编译器问题）
      - name: 构建 libvpx v1.14.0
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "📦 开始构建 libvpx v1.14.0..."
          
          # 临时清除可能干扰的环境变量
          unset CFLAGS
          unset CXXFLAGS
          unset CPPFLAGS
          
          git clone https://chromium.googlesource.com/webm/libvpx
          cd libvpx
          git checkout v1.14.0
          
          # 使用简化的配置，避免编译器检测问题
          ./configure \
            --prefix=/data/data/com.winlator/files/rootfs/usr \
            --libdir=/data/data/com.winlator/files/rootfs/usr/lib \
            --enable-shared \
            --disable-static \
            --enable-vp8 \
            --enable-vp9 \
            --disable-examples \
            --disable-tools \
            --disable-docs \
            --disable-unit-tests \
            --target=arm64-linux-gcc
          
          # 如果上面的配置失败，尝试更简化的配置
          if [ $? -ne 0 ]; then
            echo "⚠️ 标准配置失败，尝试简化配置..."
            make distclean || true
            ./configure \
              --prefix=/data/data/com.winlator/files/rootfs/usr \
              --enable-shared \
              --disable-static \
              --target=generic-gnu
          fi
          
          make -j$(nproc)
          make install
          echo "✅ libvpx v1.14.0 已安装"

      # 🛠️ 构建 GStreamer 解码器（修复 Meson 安装权限问题）
      - name: 构建 GStreamer 解码器
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "🏗️ 开始构建 GStreamer..."
          cd gstreamer
          git submodule update --init --recursive
          
          # 再次确保权限正确
          WINLATOR_ROOT="/data/data/com.winlator/files/rootfs"
          chmod -R 755 $WINLATOR_ROOT
          
          # 创建临时安装目录
          TEMP_INSTALL_DIR="/tmp/gst-install"
          mkdir -p $TEMP_INSTALL_DIR
          
          echo "⚙️ 配置 GStreamer 构建..."
          meson setup build \
            -Dprefix=/usr \
            --libdir=lib \
            -Dbase=enabled \
            -Dgood=enabled -Dugly=enabled -Dbad=enabled \
            -Dlibav=enabled -Dgpl=enabled -Ddefault_library=shared \
            -Dtests=disabled -Ddoc=disabled -Dexamples=disabled \
            -Dpython=disabled -Dnls=disabled \
            -Dgst-plugins-libav:aac=disabled \
            -Dgst-plugins-good:vpx=enabled \
            -Dgst-plugins-bad:hip=disabled -Dgst-plugins-bad:nvcodec=disabled \
            -Dgst-plugins-base:gl=disabled -Dintrospection=disabled \
            -Dgst-full=enabled \
            -Ddevtools=disabled -Dtools=disabled -Drtsp_server=disabled \
            -Dbuildtype=release
          
          echo "🔨 编译 GStreamer..."
          ninja -C build -j$(nproc)
          
          echo "📥 安装 GStreamer 到临时目录..."
          DESTDIR="$TEMP_INSTALL_DIR" ninja -C build install
          
          echo "📋 复制文件到目标位置..."
          # 复制所有文件到 Winlator 目录
          cp -r $TEMP_INSTALL_DIR/usr/* $WINLATOR_ROOT/usr/
          
          # 修复文件权限
          find $WINLATOR_ROOT/usr -type f -exec chmod 644 {} \;
          find $WINLATOR_ROOT/usr -type d -exec chmod 755 {} \;
          find $WINLATOR_ROOT/usr/bin -type f -exec chmod 755 {} \;
          find $WINLATOR_ROOT/usr/lib -name "*.so*" -exec chmod 755 {} \;
          
          echo "✅ GStreamer 构建完成"

      # 🔧 修正 ELF 文件 RPATH（优化模式）
      - name: 修正 ELF 文件 RPATH（优化模式）
        if: steps.check_build.outputs.skip_build == 'false'
        shell: bash
        run: |
          echo "🔧 开始修正 ELF 文件 RPATH（优化模式）..."
          WINLATOR_LIB="/data/data/com.winlator/files/rootfs/usr/lib"
          GST_PLUGINS_PATH="/data/data/com.winlator/files/rootfs/usr/lib/gstreamer-1.0"

          # 使用优化的RPATH
          OPTIMIZED_RPATH='$ORIGIN:$ORIGIN/..:$ORIGIN/../gstreamer-1.0:$ORIGIN/../../../gstreamer/libs/gst/base:$ORIGIN/../../../gstreamer/gst:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/pbutils:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/video:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/audio:$ORIGIN/../../../gst-plugins-base/gst-libs/gst/tag:$ORIGIN/../../../glib-2.82.2/glib:$ORIGIN/../../../glib-2.82.2/gobject:$ORIGIN/../../../glib-2.82.2/gmodule:$ORIGIN/../../../libffi/src:$ORIGIN/../../../orc/orc:/data/data/com.winlator/files/rootfs/usr/lib'

          echo "📦 扫描动态 ELF 文件中..."
          FILES=$(find "$WINLATOR_LIB" -type f | while read -r f; do
              if file "$f" | grep -q "ELF"; then
                  if readelf -l "$f" 2>/dev/null | grep -q 'INTERP'; then
                      echo "$f"
                  fi
              fi
          done)
          TOTAL=$(echo "$FILES" | grep -c . || true)
          echo "📊 检测到 $TOTAL 个动态 ELF 文件"

          [ "$TOTAL" -eq 0 ] && echo "⚠️ 未找到可修正的动态 ELF 文件，跳过" && exit 0

          # 创建修正脚本
          FIX_SCRIPT=$(mktemp)
          cat <<'EOF' > "$FIX_SCRIPT"
          f="$1"; OPTIMIZED_RPATH="$2"; WINLATOR_LIB="$3"
          echo "🔧 修正 $f"
          
          # 首先移除现有的RPATH
          patchelf --remove-rpath "$f" 2>/dev/null || true
          
          # 设置优化的RPATH
          patchelf --set-rpath "$OPTIMIZED_RPATH" "$f" 2>/dev/null || true
          
          # 设置动态链接器
          patchelf --set-interpreter "${WINLATOR_LIB}/ld-linux-aarch64.so.1" "$f" 2>/dev/null || true
          
          # 验证设置
          echo "✅ 验证 $f 的RPATH:"
          patchelf --print-rpath "$f" 2>/dev/null || echo "无法读取RPATH"
          EOF
          
          chmod +x "$FIX_SCRIPT"
          
          # 先处理前5个文件并显示详细日志
          echo "🔍 处理前5个文件（详细日志）:"
          echo "$FILES" | head -5 | while read f; do
              bash "$FIX_SCRIPT" "$f" "$OPTIMIZED_RPATH" "$WINLATOR_LIB"
          done
          
          # 批量处理剩余文件（静默模式）
          if [ "$TOTAL" -gt 5 ]; then
              echo "⚡ 批量处理剩余 $(($TOTAL - 5)) 个文件..."
              echo "$FILES" | tail -n +6 | xargs -P "$(nproc)" -I{} bash -c 'patchelf --remove-rpath "$1" 2>/dev/null || true; patchelf --set-rpath "$2" "$1" 2>/dev/null || true; patchelf --set-interpreter "$3/ld-linux-aarch64.so.1" "$1" 2>/dev/null || true' _ {} "$OPTIMIZED_RPATH" "$WINLATOR_LIB"
          fi
          
          echo "✅ ELF 文件 RPATH 修正完成（优化模式）"
          
          # 验证关键文件的RPATH
          echo "🔍 验证关键文件RPATH:"
          find "$GST_PLUGINS_PATH" -name "*.so" | head -3 | while read f; do
              echo "📄 $f:"
              patchelf --print-rpath "$f" 2>/dev/null || echo "无法读取RPATH"
          done

      # 🧪 验证构建结果
      - name: 验证构建结果
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "🧪 验证构建结果..."
          WINLATOR_LIB="/data/data/com.winlator/files/rootfs/usr/lib"
          GST_PLUGINS_PATH="/data/data/com.winlator/files/rootfs/usr/lib/gstreamer-1.0"
          
          # 检查关键文件是否存在
          echo "🔍 检查关键文件:"
          [ -f "$WINLATOR_LIB/ld-linux-aarch64.so.1" ] && echo "✅ 动态链接器存在" || echo "❌ 动态链接器缺失"
          [ -f "$WINLATOR_LIB/gstreamer-1.0/gst-inspect-1.0" ] && echo "✅ gst-inspect-1.0 存在" || echo "❌ gst-inspect-1.0 缺失"
          [ -d "$GST_PLUGINS_PATH" ] && echo "✅ GStreamer 插件目录存在" || echo "❌ GStreamer 插件目录缺失"
          
          # 统计插件数量
          PLUGIN_COUNT=$(find "$GST_PLUGINS_PATH" -name "*.so" | wc -l)
          echo "📊 GStreamer 插件数量: $PLUGIN_COUNT"
          
          # 验证gst-inspect能否运行
          echo "🔍 测试 gst-inspect-1.0:"
          if [ -f "$WINLATOR_LIB/gstreamer-1.0/gst-inspect-1.0" ]; then
              $WINLATOR_LIB/ld-linux-aarch64.so.1 $WINLATOR_LIB/gstreamer-1.0/gst-inspect-1.0 --version && echo "✅ gst-inspect 版本检查成功" || echo "❌ gst-inspect 版本检查失败"
          else
              echo "⚠️ gst-inspect-1.0 不存在，跳过测试"
          fi
          
          # 检查关键插件
          echo "🔍 检查关键插件:"
          for plugin in coreelements apetag vp8dec vp9dec; do
              if [ -f "$WINLATOR_LIB/gstreamer-1.0/gst-inspect-1.0" ]; then
                  if $WINLATOR_LIB/ld-linux-aarch64.so.1 $WINLATOR_LIB/gstreamer-1.0/gst-inspect-1.0 $plugin >/dev/null 2>&1; then
                      echo "✅ 插件 $plugin 可用"
                  else
                      echo "❌ 插件 $plugin 不可用"
                  fi
              else
                  echo "⚠️ 无法检查插件 $plugin (gst-inspect 缺失)"
              fi
          done

      # 🧱 打包构建结果
      - name: 打包 usr 目录
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "📦 开始打包构建结果..."
          cd /data/data/com.winlator/files/rootfs
          TAR_NAME="winlator-gst-decoders-${VERSION}-compiler-fixed.tar.gz"
          
          # 创建打包列表，排除不必要的文件
          echo "🗂️ 创建打包清单..."
          find usr -type f | head -10 > /tmp/file_list.txt
          echo "... (省略其他文件)" >> /tmp/file_list.txt
          echo "📄 打包前文件列表样本:"
          cat /tmp/file_list.txt
          
          # 打包
          tar -czvf /home/runner/$TAR_NAME usr
          
          # 记录构建信息
          echo "$LATEST_COMMIT" > ${{ env.LAST_COMMIT_FILE }}
          echo "TAR_NAME=$TAR_NAME" >> $GITHUB_ENV
          
          # 显示打包信息
          echo "📊 打包完成: $TAR_NAME"
          du -h /home/runner/$TAR_NAME

      # ⬆️ 上传构建产物
      - name: 上传构建产物
        if: steps.check_build.outputs.skip_build == 'false'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.TAR_NAME }}
          path: /home/runner/${{ env.TAR_NAME }}
          retention-days: 30

      # 💾 上传提交记录
      - name: 上传提交记录
        if: steps.check_build.outputs.skip_build == 'false'
        uses: actions/upload-artifact@v4
        with:
          name: last-gst-commit
          path: ${{ env.LAST_COMMIT_FILE }}
          retention-days: 365

      # 🚀 发布到 GitHub Release
      - name: 发布到 GitHub Release
        if: steps.check_build.outputs.skip_build == 'false'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.VERSION }}-compiler-fixed
          name: "GStreamer Decoders ${{ env.VERSION }} Compiler Fixed (aarch64, Winlator)"
          body: |
            ## 🎯 GStreamer 开发版本构建 - 编译器修复版

            ### 📋 构建信息
            - **版本**: `${{ env.VERSION }}`
            - **架构**: aarch64
            - **环境**: Winlator
            - **提交**: [`${{ env.COMMIT_SHORT }}`](https://gitlab.freedesktop.org/gstreamer/gstreamer/-/commit/${{ env.LATEST_COMMIT }})
            - **提交日期**: ${{ env.COMMIT_DATE }}
            - **作者**: `${{ env.AUTHOR }}`
            - **构建时间**: `${{ env.BUILD_DATE }}`

            ### 🛠️ 构建特性
            - ✅ Base / Good / Ugly / Bad / LibAV / GPL 全启用
            - ✅ VP8/VP9 解码支持 (libvpx v1.14.0)
            - ❌ RTSP Server 已禁用
            - 🏗️ 构建类型: Release
            - 🔗 RPATH: 使用优化的相对路径模式

            ### 🔧 修复内容
            - ✅ 修复 libvpx 编译器问题：清除干扰环境变量
            - ✅ 修复 Meson 安装权限问题：使用临时目录安装
            - ✅ 优化 RPATH 设置，解决动态库加载问题
            - ✅ 改进目录权限管理

            ### 📦 文件信息
            - 文件名: `${{ env.TAR_NAME }}`
            - 动态链接器: `/data/data/com.winlator/files/rootfs/usr/lib/ld-linux-aarch64.so.1`
            - 优化的 RPATH: `$ORIGIN:$ORIGIN/..:$ORIGIN/../gstreamer-1.0:...`
            - 安装路径: `/data/data/com.winlator/files/rootfs/usr`
            
            ### 📥 安装说明
            1. 解压到 Winlator 的 rootfs 目录：
            ```bash
            tar -xzvf ${{ env.TAR_NAME }} -C /data/data/com.winlator/files/rootfs/
            ```
            
            ### 🧪 测试命令
            ```bash
            # 设置环境变量
            export LD_LIBRARY_PATH=/data/data/com.winlator/files/rootfs/usr/lib
            export PATH=/data/data/com.winlator/files/rootfs/usr/bin:$PATH
            
            # 测试 GStreamer
            gst-inspect-1.0 --version
            gst-inspect-1.0 vp8dec
            gst-inspect-1.0 vp9dec
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 📊 构建总结
      - name: 构建总结
        if: steps.check_build.outputs.skip_build == 'false'
        run: |
          echo "🎉 构建完成总结"
          echo "========================"
          echo "✅ 版本: ${{ env.VERSION }}"
          echo "✅ 提交: ${{ env.COMMIT_SHORT }}"
          echo "✅ 文件: ${{ env.TAR_NAME }}"
          echo "✅ 时间: ${{ env.BUILD_DATE }}"
          echo "✅ 状态: 编译器问题已修复"
          echo "========================"
          echo "所有构建步骤已完成！"